/**
 * Константы создания фигур. Новые фигуры получают эти значения;
 * цвет потом меняется в панели свойств.
 */

/** Заливка новых фигур (sky-400). */
export const DEFAULT_SHAPE_FILL = '#38bdf8'

/** Минимальный размер фигуры: меньше — драг считается кликом и отбрасывается. */
export const MIN_DRAW_SIZE = 4

/** Палитра быстрого выбора цвета в панели свойств. */
export const PRESET_COLORS = [
  '#38bdf8',
  '#f472b6',
  '#a3e635',
  '#facc15',
  '#fb7185',
  '#a78bfa',
  '#34d399',
  '#f8fafc',
] as const

/** Размер маркера рамки выделения в экранных пикселях (не зависит от зума). */
export const SELECTION_HANDLE_SIZE = 7
